## 1. 环境准备与基础框架

- [x] 1.1 在项目中配置新增依赖：`streamlit`, `plotly` 等（如更新 `requirements.txt` 或 `pyproject.toml`）
- [x] 1.2 创建前端代码目录结构（如 `app/` 及其子目录 `pages/`, `components/`）
- [x] 1.3 编写 `app/run_dashboard.py` 作为 Streamlit 应用的主入口文件，并实现侧边栏系统导航菜单

## 2. 行情看板模块 (Market Monitor)

- [x] 2.1 编写适配 UI 调用的本地 ETF 行情数据获取及缓存（`@st.cache_data`）逻辑
- [x] 2.2 实现指定周期内的涨跌幅排行榜表格组件
- [x] 2.3 使用 Plotly 实现多品种历史走势叠加对比的交互式图表组件

## 3. 轮动池管理模块 (Pool Manager)

- [x] 3.1 实现配置文件的读取以及当前自选池标的列表展示
- [x] 3.2 调用底层指标计算引擎，实现标的动量评分计算
- [x] 3.3 在界面上渲染输出带有分数的动量排名列表

## 4. 策略回测面板 (Backtest Panel)

- [x] 4.1 实现策略选择和动态参数输入的表单组件
- [x] 4.2 编写连接 UI 层与底层回测引擎（`engine.py`）的执行器函数，并处理 UI 加载状态
- [x] 4.3 使用 Plotly 绘制带有买卖点标记的净值曲线交互图表
- [x] 4.4 从回测引擎提取结果并渲染标准化的绩效统计指标表格

## 5. 历史记录查看模块 (History Viewer)

- [x] 5.1 编写本地 `results/` 目录的文件扫描逻辑，提取历史回测记录列表（时间、策略、品种）
- [x] 5.2 实现历史数据的加载与解析
- [x] 5.3 复用净值曲线图表组件和绩效表格组件，展示所选历史记录的详细结果

## 6. 文档更新

- [x] 6.1 更新项目根目录的 `README.md`，添加关于如何启动和使用可视化仪表盘的说明及目录结构变化
