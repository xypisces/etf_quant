## 1. 排名缓存持久化

- [x] 1.1 实现排名结果保存函数：将排名 DataFrame 连同元信息（计算时间、动量周期、范围类型）保存到 `data/momentum_ranking_cache.parquet`
- [x] 1.2 实现排名结果加载函数：从 Parquet 文件读取缓存排名数据和元信息，文件不存在时返回 None
- [x] 1.3 实现全量计算时截取前 50 名再保存的逻辑

## 2. 下拉筛选器

- [x] 2.1 在计算按钮上方添加 `st.multiselect` 下拉多选组件，选项格式为 `{code} - {name}`，支持搜索
- [x] 2.2 实现筛选逻辑：有选中标的时仅计算选中标的，无选择时计算全量
- [x] 2.3 更新页面提示文本，根据筛选状态显示「将对 N 只 ETF 计算」

## 3. 重新计算 Top50

- [x] 3.1 添加"🔄 重新计算 Top50"按钮，仅在存在缓存时启用
- [x] 3.2 实现点击后从缓存读取前 50 名标的代码，对其执行增量更新 + 动量重算
- [x] 3.3 重算完成后更新排名展示并覆盖缓存

## 4. 页面加载时展示缓存

- [x] 4.1 页面加载时自动检测并读取缓存文件，展示上次排名结果
- [x] 4.2 显示缓存元信息：计算时间、动量周期、范围类型（全量/筛选）

## 5. 收尾

- [x] 5.1 更新 README.md，记录新增的排名缓存功能和使用方式
